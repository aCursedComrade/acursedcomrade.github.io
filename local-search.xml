<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>&quot;/proc&quot; file system | Enumerating for a foothold</title>
    <link href="/2023/03/proc-enumeration/"/>
    <url>/2023/03/proc-enumeration/</url>
    
    <content type="html"><![CDATA[<h2 id="What-is-“-x2F-proc”"><a href="#What-is-“-x2F-proc”" class="headerlink" title="What is “&#x2F;proc”?"></a>What is “&#x2F;proc”?</h2><p>The <code>proc</code> file system, which usually lies in <code>/proc</code> directory of a Linux system, contains information about the runtime of the system and all processes on the system. This information is stored in the form of files or file-like objects which can be read from using simple text readers such <code>cat</code> or <code>grep</code> and can be modified by high-privileged users and process for different purposes.</p><p>In case of a vulnerability like <strong>LFI (Local File Inclusion)</strong>, <strong>command injection</strong> or similar vector, this makes it a useful location for recon&#x2F;enumeration for an attacker or from a CTF standpoint which I can personally relate to when solving challenges.</p><h2 id="A-little-scenario"><a href="#A-little-scenario" class="headerlink" title="A little scenario"></a>A little scenario</h2><p>Throughout this post, I will be using a <em>smol</em> <strong>python Flask</strong> app that is deliberately vulnerable to an LFI to demonstrate the attack vector. The code for the web app is given below, make sure to run it in a contained environment:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> flask <span class="hljs-keyword">import</span> Flask, request<br>app = Flask(__name__)<br><br><span class="hljs-meta">@app.route(<span class="hljs-params"><span class="hljs-string">&#x27;/&#x27;</span></span>)</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">index</span>():<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&#x27;Hello there!\n&#x27;</span><br><br><span class="hljs-meta">@app.route(<span class="hljs-params"><span class="hljs-string">&#x27;/read&#x27;</span></span>)</span><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">read</span>():<br>    file = request.args.get(<span class="hljs-string">&#x27;fn&#x27;</span>)<br>    <span class="hljs-keyword">with</span> <span class="hljs-built_in">open</span>(<span class="hljs-string">f&#x27;/tmp/<span class="hljs-subst">&#123;file&#125;</span>&#x27;</span>, <span class="hljs-string">&#x27;rb&#x27;</span>) <span class="hljs-keyword">as</span> f: <span class="hljs-comment"># Classic LFI</span><br>        data = f.read()<br>        <span class="hljs-keyword">return</span> data<br><br><span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:<br>    app.run()<br></code></pre></td></tr></table></figure><p>If you haven’t already installed <strong>Flask</strong> library, you can do so by executing the command:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">pip install Flask<br></code></pre></td></tr></table></figure><p>For demonstration, I’m using the <code>export</code> command to create a few environment variables before we run the application. To so this and run the script, we can execute the following:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell">export SOME_VAR=&quot;aVariableHere&quot; \<br>SECRET=&quot;YouShouldntBeReadingThis&quot; \<br>VERSION=&quot;6.9.0&quot; \<br>DB_URL=&#x27;mysql://dbuser:P@$$w0rd@localhost/webapp&#x27;; flask --app app run<br></code></pre></td></tr></table></figure><p>Make sure to execute the command in the same directory as the script. If executed successfully, our vulnerable web app should be listening for new connections.</p><p><img src="/../img/flask_start.png" alt="Flask app configured and listening"></p><h2 id="The-attack-begins"><a href="#The-attack-begins" class="headerlink" title="The attack begins"></a>The attack begins</h2><p>First things first, let’s confirm the LFI by querying the classic <code>/etc/passwd</code>. If we look at the source code again, the app is trying to read from <code>/tmp</code> with the filename passed <strong>directly</strong> in to the expression without any sanitation. We only need a single set of <code>..</code> operator to access the file system root. So our malicious query will be:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">curl &#x27;localhost:5000/read?fn=../etc/passwd&#x27;<br></code></pre></td></tr></table></figure><p><img src="/../img/flask_etc_passwd.png" alt="The content of /etc/passwd as the response"></p><p>Great! (or not so great <em>wink</em>) This means that we can now read all files in the systems which can be read by the system account running the web app. An attacker can now try to read other various configuration files that presumably on default locations or other files and gather as much as information to gain a foothold.</p><h3 id="“-x2F-proc”-structure"><a href="#“-x2F-proc”-structure" class="headerlink" title="“&#x2F;proc” structure"></a>“&#x2F;proc” structure</h3><ul><li>You can break down the entries into two major categories. One is <strong>kernel (or system) information</strong> and <strong>process specific information</strong>.</li><li>The numbered directories you see are made for each process, thus contains process information. The number represents the process ID (PID) of individual process.</li><li>The remaining directories and file-like objects contains kernel information (but there are 4 interesting exceptions which will be discussed below).</li><li>Another interesting fact is that the <code>/proc</code> <strong>occupies no disk space at all (or at least a very minimal amount)</strong>, the kernel (or the system) generates the necessary information dynamically when a programs access specific entries within <code>/proc</code>.</li><li>With the exceptions of few entries, the contents of <code>/proc</code> are globally readable. But as an attacker, we only have to look at few interesting entries.</li></ul><p><img src="/../img/proc_contents.png" alt="Contents of /proc"></p><div class="note note-success">            <p>This post will go over some entries briefly. You can read on the complete documentation for <code>/proc</code> file system and its entries from the <strong>Linux documentation</strong><sup id="fnref:1" class="footnote-ref"><a href="#fn:1" rel="footnote"><span class="hint--top hint--rounded" aria-label="[The Linux Documentation Project](https://tldp.org/LDP/Linux-Filesystem-Hierarchy/html/proc.html)">[1]</span></a></sup> or from <strong>man pages</strong><sup id="fnref:2" class="footnote-ref"><a href="#fn:2" rel="footnote"><span class="hint--top hint--rounded" aria-label="man 5 proc (in your terminal) or [man7 on the web](https://man7.org/linux/man-pages/man5/proc.5.html)">[2]</span></a></sup> in your own terminal. More links are available at the end of the post.</p>          </div><div class="note note-info">            <p>In this scenario, we are using an LFI vulnerability to read files off the system. We can read <strong>entries (files or file-like objects)</strong> in <code>/proc</code> and in other parts of the file system, but we cannot read <strong>directories</strong> which is the expected behavior. However, a capable attacker can automate a brute-force attack with a list of possible file&#x2F;directory names to discover files in the file system.</p>          </div><h3 id="Kernel-information"><a href="#Kernel-information" class="headerlink" title="Kernel information"></a>Kernel information</h3><ul><li><code>/proc/cpuinfo</code><ul><li>Returns information regarding individual CPU cores available to the system. (Response can be quite long depending on the number of cores)</li></ul></li></ul><p><img src="/../img/proc_cpuinfo.png" alt="Response of /proc/cpuinfo (truncated)"></p><ul><li><code>/proc/meminfo</code><ul><li>Returns information about the system memory. This includes total memory, used memory, free memory, etc.</li></ul></li></ul><p><img src="/../img/proc_meminfo.png" alt="Response of /proc/meminfo (truncated)"></p><ul><li><code>/proc/version</code><ul><li>Returns the kernel version information.</li></ul></li></ul><p><img src="/../img/proc_version.png" alt="Response of /proc/version"></p><ul><li><code>/proc/partitions</code><ul><li>Returns the partition table known to the system.</li></ul></li></ul><p><img src="/../img/proc_partitions.png" alt="Response of /proc/partitions"></p><p>As mentioned in <a href="#proc-structure">&#x2F;proc structure</a>, there a few interesting links that’s available within the <code>/proc</code>. They are listed below:</p><ul><li><p><code>/proc/self</code></p><ul><li>This is a <em>magical</em> link that automatically points to the relevant <code>/proc/&lt;PID&gt;/</code> <strong>directory</strong> of the process(s) that’s currently accessing the entry. This is made as a convenient way of accessing own process information. Can be easily used in attacks such as LFI to quickly expose information about the current process.</li></ul></li><li><p><code>/proc/mounts</code></p><ul><li>Like <code>/proc/self</code>, this link points to the <code>/proc/&lt;PID&gt;/mounts</code> entry of the own process. This contains information about the file system mounts that accessible by the process (Refer <code>/proc/mounts</code> in man pages<sup id="fnref:2" class="footnote-ref"><a href="#fn:2" rel="footnote"><span class="hint--top hint--rounded" aria-label="man 5 proc (in your terminal) or [man7 on the web](https://man7.org/linux/man-pages/man5/proc.5.html)">[2]</span></a></sup>). This can include network shares that are mounted on the system.</li><li>For example, below you can see a mount that is created by <a href="https://kdeconnect.kde.org/">KDE Connect</a> that is exposing some folders in my android device. In the next screenshot, I’m accessing the <code>Documents/message.txt</code> that results in information disclosure.</li></ul></li></ul><p><img src="/../img/proc_mounts.png" alt="Response of /proc/mounts"></p><p><img src="/../img/secret_message.png" alt="Secrets exposed!!!"></p><ul><li><code>/proc/net/</code><ul><li>Like <code>/proc/mounts</code>, this link points to the <code>/proc/&lt;PID&gt;/net/</code> <strong>directory</strong> of the own process. The entries within this directory holds different kinds of information about the networking stack that is accessible by the process (Refer <code>/proc/net</code> in man pages<sup id="fnref:2" class="footnote-ref"><a href="#fn:2" rel="footnote"><span class="hint--top hint--rounded" aria-label="man 5 proc (in your terminal) or [man7 on the web](https://man7.org/linux/man-pages/man5/proc.5.html)">[2]</span></a></sup>).</li><li>For example, <code>arp</code> entry holds the ARP cache of the system which can be used to discover internal hosts of the network. <code>tcp</code> and <code>udp</code> entries have relevant connections that are established or listening on the system (in hex format).</li></ul></li></ul><p><img src="/../img/proc_net_arp.png" alt="Response of /proc/net/arp"></p><ul><li><code>/proc/threads-self</code><ul><li>Like the above magic links, this point to the relevant <code>/proc/&lt;PID&gt;/task/&lt;TID&gt;/</code> <strong>(process thread) directory</strong> of the own process, if it is being accessed by a thread. The TID here is similar to PID and threads are accessible via <code>/proc/&lt;TID&gt;</code> as well. However, threads are different from processes in computing.</li></ul></li></ul><p><img src="/../img/proc_task.png" alt="Threads in my &quot;Terminator&quot; session"></p><h3 id="Process-information"><a href="#Process-information" class="headerlink" title="Process information"></a>Process information</h3><div class="note note-info">            <p>In this scenario, we can use <code>/proc/self/</code> link to expose information about the web app process without guessing the PID. But it is possible to brute-force PID to find processes of interest.</p>          </div><ul><li><code>/proc/&lt;PID&gt;/comm</code> &amp; <code>/proc/&lt;PID&gt;/cmdline</code><ul><li><code>comm</code> and <code>cmdline</code> holds the base name of the command and the complete command used to execute the command respectively. <code>cmdline</code> will spit out the whole with the spaces in between arguments replaced by a null byte ‘\0’. We can pass this through a replace function to restore the spaces. What’s important here that the command line arguments can contain password (This is bad practice).</li></ul></li></ul><p><img src="/../img/proc_cmd.png" alt="Response of /proc/self/cmd &amp; /proc/self/cmdline"></p><ul><li><code>/proc/&lt;PID&gt;/status</code><ul><li>This entry contains an array of different kinds of information. <code>Name</code>, <code>Pid</code>, <code>PPid</code> (Parent PID), <code>Uid</code> (Effective user ID), <code>Gid</code> (Effective group ID) and <code>Groups</code> (Groups the process belongs to) are some important fields out of it for enumeration.</li></ul></li></ul><p><img src="/../img/proc_status.png" alt="Response of /proc/self/status"></p><ul><li><code>/proc/&lt;PID&gt;/environ</code><ul><li>This is the <strong>most important one</strong> in my opinion. This holds the <em>initial</em> environment variables that was used to execute the process with. By initial, it means that if any changes happen to these during the process lifetime, it won’t be reflected here.</li><li>Often for production or in development, environment variables are used to hold configuration information and usually these contain sensitive information like secret keys, authentication tokens, database credentials, etc.</li><li>For example, we have our dummy variables we set up earlier dumped here (the newlines ‘\n’ are replaces by null bytes ‘\0’ here as well) (I have also truncated the output to get rid of the unnecessary variables of my ZSH config).</li></ul></li></ul><p><img src="/../img/proc_environ.png" alt="Response of /proc/self/environ"></p><ul><li><code>/proc/&lt;PID&gt;/cwd/</code><ul><li>This a symbolic link that points to the <strong>current working directory</strong> of the process. Can sometimes be used as a shortcut in context of web app path.</li></ul></li></ul><p><img src="/../img/proc_cwd.png" alt="Content of /proc/self/cwd/"></p><ul><li><code>/proc/&lt;PID&gt;/exe</code><ul><li>This entry points directly to the executable command that is used run the command. Reading this will output raw binary data. Useful when you are dealing with custom standalone binaries during challenges.</li></ul></li></ul><p><img src="/../img/proc_exe.png" alt="Content of /proc/self/exe"></p><ul><li><code>/proc/&lt;PID&gt;/fd/</code><ul><li>This <strong>directory</strong> contains symbolic links to each file that are open in the process, named using numbers (Numbers correlate to the entries in <code>fdinfo/</code> directory). Except the first three (0, 1, 2) which are standard input, output, error respectively, files can be queried to view the contents inside it. Sometimes it can be used to execute code in context of the web app<sup id="fnref:3" class="footnote-ref"><a href="#fn:3" rel="footnote"><span class="hint--top hint--rounded" aria-label="[LFI to RCE via /proc/*/fd](https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/File%20Inclusion/README.md#lfi-to-rce-via-procfd)">[3]</span></a></sup>.</li></ul></li></ul><p><img src="/../img/proc_fd.png" alt="Content of /proc/self/fd/"></p><h2 id="Concluding-thoughts"><a href="#Concluding-thoughts" class="headerlink" title="Concluding thoughts"></a>Concluding thoughts</h2><p>That’s about it for my first blog post :) This post covered a few interesting locations in the Linux <code>/proc</code> file system that an attacker with some read access on the system during compromise or even during post exploitation phase can enumerate to uncover information. You can refer the links in the below sections for more information about the topic.</p><p>But this is a small part of an attack chain. Depending on the context of the scenario, an attacker will think outside box to carry out the attack chain and compromise a system. <strong>YOU</strong> should think out of the box too.</p><p>Thank you for reading <strong>o7</strong></p><p><img src="https://media.tenor.com/KmPFMGQ07-4AAAAd/hffgf.gif" alt="Cat hacking the system"></p><h2 id="Extra-reading"><a href="#Extra-reading" class="headerlink" title="Extra reading"></a>Extra reading</h2><ul><li><a href="https://idafchev.github.io/enumeration/2018/03/05/linux_proc_enum.html">Linux enumeration with read access only</a></li><li><a href="https://www.netspi.com/blog/technical/web-application-penetration-testing/directory-traversal-file-inclusion-proc-file-system/">Directory Traversal, File Inclusion, and The Proc File System</a></li></ul><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><section class="footnotes"><div class="footnote-list"><ol><li><span id="fn:1" class="footnote-text"><span><a href="https://tldp.org/LDP/Linux-Filesystem-Hierarchy/html/proc.html">The Linux Documentation Project</a><a href="#fnref:1" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:2" class="footnote-text"><span>man 5 proc (in your terminal) or <a href="https://man7.org/linux/man-pages/man5/proc.5.html">man7 on the web</a><a href="#fnref:2" rev="footnote" class="footnote-backref"> ↩</a></span></span></li><li><span id="fn:3" class="footnote-text"><span><a href="https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/File%20Inclusion/README.md#lfi-to-rce-via-procfd">LFI to RCE via &#x2F;proc&#x2F;*&#x2F;fd</a><a href="#fnref:3" rev="footnote" class="footnote-backref"> ↩</a></span></span></li></ol></div></section>]]></content>
    
    
    
    <tags>
      
      <tag>linux</tag>
      
      <tag>ctf</tag>
      
      <tag>attack</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World!</title>
    <link href="/2022/12/hello/"/>
    <url>/2022/12/hello/</url>
    
    <content type="html"><![CDATA[<h2 id="Hello-World-D"><a href="#Hello-World-D" class="headerlink" title="Hello World :D"></a>Hello World :D</h2><p>This is my shot at making a blog and writing about stuff. You can expect me to write about tech stuff, cybersecurity (when I am a professional <em>‘wink’</em>), games ??? or anything else that I may find interesting.</p><p>And that’s about it for this post. You can take a look at the <a href="/about">about page</a> for more information about me.<br>Thanks for reading!</p><p><img src="https://media.tenor.com/dI7PKF421EgAAAAd/thank-you-bow.gif" alt="Thank you!"></p>]]></content>
    
    
    
    <tags>
      
      <tag>personal</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
